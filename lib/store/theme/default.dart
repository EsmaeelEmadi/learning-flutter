import 'package:flutter/material.dart';

class InitialTheme {
  static Brightness brightness = Brightness.light;
  static bool showMainMenu = false;
  static bool isSearch = false;
}

// final ThemeData initialTheme = ThemeData.dark();
